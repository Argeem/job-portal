info:
  name: Get Public Profile
  type: http
  seq: 3

http:
  method: GET
  url: "{{localhost}}/api/user/:id"
  params:
    - name: id
      value: "{{user_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
