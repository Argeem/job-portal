info:
  name: Upload Image
  type: http
  seq: 4

http:
  method: POST
  url: "{{localhost}}/api/auth/upload-image"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
