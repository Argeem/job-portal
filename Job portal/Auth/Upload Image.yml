info:
  name: Upload Image
  type: http
  seq: 4

http:
  method: POST
  url: "{{localhost}}/api/auth/upload-image"
  body:
    type: multipart-form
    data:
      - name: image
        type: file
        value:
          - C:\Users\sitth\Downloads\avatar1.jpg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
